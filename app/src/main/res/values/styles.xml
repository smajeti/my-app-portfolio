<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
    </style>

    <style name="Nano.Portfolio.Button" parent="@android:style/Widget.Button" >
        <item name="android:background">@color/btn_background_color</item>
        <item name="android:textColor">@color/btn_textcolor</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">@dimen/main_page_btn_width</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
</resources>
